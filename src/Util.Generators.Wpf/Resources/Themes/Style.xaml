<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:hc="https://handyorg.github.io/handycontrol">

    <Style x:Key="LeftSideMenu" TargetType="hc:SideMenu" BasedOn="{StaticResource SideMenuBaseStyle}">
        <Setter Property="hc:ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="hc:ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="hc:ScrollViewer.CanContentScroll" Value="true" />
        <Setter Property="hc:ScrollViewer.PanningMode" Value="Both" />
        <Setter Property="BorderThickness" Value="0,1,0,0"></Setter>
    </Style>

    <Style x:Key="LeftSideMenuIcon" TargetType="Path">
        <Setter Property="Width" Value="20"/>
        <Setter Property="Height" Value="20"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Stretch" Value="Uniform"/>
        <Setter Property="Fill" Value="{DynamicResource PrimaryTextBrush}"></Setter>
    </Style>
    
    <Style x:Key="LeftSideMenuItemHeader" TargetType="hc:SideMenuItem" BasedOn="{StaticResource SideMenuItemHeaderBaseStyle}">
        <!-- <Setter Property="Margin" Value="15,0,0,0"></Setter> -->
    </Style>

</ResourceDictionary>
